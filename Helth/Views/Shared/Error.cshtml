@model ErrorViewModel
@{
    ViewData["Title"] = "خطأ";
}

<h1 class="certificate-title">حدث خطأ</h1>
<p class="empty-state">عذراً، حدث خطأ أثناء معالجة الطلب.</p>

@if (Model.ShowRequestId)
{
    <p>
        <strong>رقم الطلب:</strong> <code>@Model.RequestId</code>
    </p>
}
